{
  "name": "scopemaker-frontend-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Tests for the browser JavaScript. The application itself is Python; this exists only so the editor's interactive behaviour is not verified by hand.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
